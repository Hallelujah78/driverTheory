import { AiOutlineCheck, AiOutlineClose } from "react-icons/ai";

const ENUM_STATES = {
  correct: <AiOutlineCheck className="correct" />,
  incorrect: <AiOutlineClose className="incorrect" />,
  default: null,
};

export default ENUM_STATES;
